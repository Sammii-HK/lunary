import UIKit

class SceneDelegate: UIResponder, UIWindowSceneDelegate {

    var window: UIWindow?

    // Window and root view controller are set up automatically by Main.storyboard.
    // All UIWindowSceneDelegate lifecycle methods are optional — no stubs needed.
}
