import numerologyData from '@/data/numerology.json';

export const angelNumbers = numerologyData.angelNumbers ?? {};
export const lifePathNumbers = numerologyData.lifePathNumbers ?? {};

export default numerologyData;
